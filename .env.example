# Database Connection
# Format: postgres://user:password@host:port/database_name
# Replace the values below with your specific Postgres credentials
DATABASE_URL=postgres://postgres:postgres@localhost:5432/dnd_character_db

# Currency Settings
# Set to "true" to use Electrum (EP) instead of Silver (SP) in currency conversions
# Default: false (uses Silver: CP → SP → GP → PP)
# When true: CP → EP → GP → PP
NEXT_PUBLIC_USE_ELECTRUM=false
